api_platform:
    title: 'SEO Audit API'
    version: '1.0.0'

    swagger:
        api_keys:
            JWT:
                name: Authorization
                type: header
    formats:
        json: ['application/json']
        jsonld: ['application/ld+json']